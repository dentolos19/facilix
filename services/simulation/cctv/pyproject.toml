[project]
name = "cctv-simulator"
version = "0.1.0"
description = "CCTV Simulator — loops local MP4 files and publishes RTSP/RTMP streams via MediaMTX"
requires-python = ">=3.12"
dependencies = [
    "fastapi>=0.115.0",
    "uvicorn[standard]>=0.34.0",
]
