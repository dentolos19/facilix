[project]
name = "facilix-monitoring"
version = "1.0.0"
requires-python = ">=3.13"
dependencies = ["fastapi", "uvicorn", "httpx", "pydantic"]

[project.scripts]
servicelocate = { callable = "main:app" }

[dependency-groups]
dev = ["ruff"]

[tool.ruff]
line-length = 120
