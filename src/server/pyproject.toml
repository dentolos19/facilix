[project]
name = "facilix-server"
version = "1.0.0"
requires-python = ">=3.12"
dependencies = [
  "fastapi",
  "httpx",
  "numpy",
  "opencv-python-headless",
  "pydantic",
  "uvicorn",
]

[dependency-groups]
dev = ["ruff", "ty"]

[tool.ruff]
line-length = 120
