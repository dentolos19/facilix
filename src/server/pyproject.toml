[project]
name = "facilix-server"
version = "1.0.0"
requires-python = ">=3.13"
dependencies = ["fastapi", "uvicorn", "httpx", "pydantic"]

[dependency-groups]
dev = ["ruff"]

[tool.ruff]
line-length = 120
